require('dotenv').config(); const { db } = require('./db'); const id=process.env.INITIAL_OWNER_ROBLOX_ID; if(!id){console.log('Set INITIAL_OWNER_ROBLOX_ID in .env first.'); process.exit(0);} db.prepare('INSERT INTO users(roblox_id,username,role,is_allowed) VALUES(?,?,?,1) ON CONFLICT(roblox_id) DO UPDATE SET role="owner", is_allowed=1').run(String(id),'Owner','owner'); console.log('Owner seeded:', id);
